package br.com.jtsilva.Arrays;

public class ArrayTeste {
    public static void main(String[] args) {
        // Declaração de array
        int[] array;

        // Declarando e construindo um Array
        String[] nomes = new String[10];

        // Declarando e inicializando Array
        char[] letras = { 'a', 'b', 'c', 'd', 'e' };
        System.out.printf("Tamanho do Array 'Letras': %d posições.", letras.length);
    }
}
